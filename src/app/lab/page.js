import LabClient from "./LabClient";

export const metadata = {
  title: "Project Lab | Batıkan Bora Ormancı",
  description:
    "An interactive Three.js project browser for Batıkan Bora Ormancı's work.",
};

export default function ProjectLabPage() {
  return <LabClient />;
}
